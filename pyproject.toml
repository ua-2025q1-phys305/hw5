[project]
name = "phys305_hw5"
version = "0.0.0"
dependencies = [
    "nbconvert",
    "pytest",
]

[project.entry-points."bilby.plugin"]
mymcmc = "mypackage.mymcmc"

[project.entry-points."bilby.samplers"]
mysampler = "phys305_hw5.a2:MySampler"
